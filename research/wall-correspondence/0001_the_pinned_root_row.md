# 0001 — The pinned root row: the bias theorem is their masslessness theorem

> **AI-generated, not peer-reviewed** — a correspondence record, not a
> new filter result. Everything cited from this repository is this
> repository's own measured work; everything from the sibling is
> theirs (`quantum-mechanics`, foundations/0096 mirrors this file).

The first verified row of the homomorphism between this family and
the sibling physics program, and it runs through
[`ode-filter/0041`](../ode-filter/0041_a_climbing_bias_is_a_pinned_root.md):
*a climbing bias is a pinned root*.

## The row

This repository proved, from data: a bias can live only in a
characteristic root pinned at z = 1 **exactly**; a free ML fit lands
the root at 1 ± ε with ε = O(1/n); a root is an exponent, so the
additive climb is rendered geometric — catastrophic at horizon; the
cure is structural (`fit(unit_roots=d)`), and the prequential ledger
reads *right pin free (+0.0003), wrong pin loud (−0.148)*.

The sibling program has the same theorem wearing field-theory
clothes, verified exactly on their side this session:

| this repository | the sibling |
|---|---|
| root pinned at z = 1 by construction | a symmetry-protected massless mode (their graviton) |
| Σαᵢ = 1 as shift equivariance | conservation of probability: their trivial channel has transfer r₀ = 1 for *any* model — the automatic pin |
| free fit's 1 ± ε root | mass generated by an imperfectly-held symmetry — their lattice graviton was massive when its criticality was *tuned* (their 0056) and became massless only when the symmetry was held *by construction* (their 0063) |
| a free fit cannot hold the root | a generic measure has every nontrivial transfer eigenvalue strictly below 1 — "mass is generic," verified over random weights |
| right pin free / wrong pin loud; damage lands in Q̂ and long-horizon variance | true constraints cost nothing; false ones displace the fitted couplings by orders — their renormalization in this repository's words |
| the u = e₁ commitment's casualty | a frame commitment silencing a sector (their "(1,0) mode") — *flagged resemblance, unverified* |

## Two consequences worth having here

**The prequential floor travels.** This repository answers "what
order p?" from below — prequential log-loss climbs to a floor and is
flat above, with AIC/BIC refused because a complexity penalty imports
a free parameter into the question. The sibling has an undetermined
representation cutoff ("where to stop counting spins") that controls
their strongest quantitative claim. The port: pose their cutoff as a
**minimal-support floor** of an exact linear system, expect flatness
above, fit nothing. Their equations, this repository's epistemology.

**The AR(1) closure has a sibling.** The walking filter's one
irreducible assumption — log-variances follow AR(1), a closed
one-parameter family for how confidence moves, plus a persistence
φ — corresponds to their heat-kernel closure: after one
renormalization step their whole model family collapses to one
scalar with a flow map (closure measured at 1e−4 on their side).
Same shape of minimal assumption; their flow coefficient
(c ≈ 0.13/blocking) is the number their side of experiment 2 (the
decimation cascade) should be compared against.

## Status

Recorded as the dictionary's first *verified* row (both sides
computed, this session, sibling module `foundations/output/0086`).
No new experiment run here; the planned experiments in
[`SUMMARY.md`](SUMMARY.md) stand, with experiment 2's target now
carrying a concrete number.
