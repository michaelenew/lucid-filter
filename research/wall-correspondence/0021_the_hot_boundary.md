# 0021 — The hot boundary: Unruh for the half-web observer

> **AI-generated, not peer-reviewed.** The horizon front, unblocked
> at the wedge tier. Code: `0021_the_hot_boundary.py`.

Through the time tier (Euclidean = smoother), the sibling's wedge
theorem (their 0083) reads in filter terms: **the joint posterior of
a massless Gaussian bank, marginalized to an observer holding half
the web, is a thermal ensemble with respect to the boost
reweighting.** Verified compactly: the reduced half-chain state
matches the boost-thermal covariances at β = 2π to **1e−4** in both
⟨xx⟩ and ⟨pp⟩, while β = π and β = 4π miss by 21% and 6% — the
temperature scan is decisive. A filter that holds half the web
experiences the other half as heat at 1/2π per rapidity: the
horizon is hot, operationally, for the record-side observer.

(A first-cut bug worth keeping: putting the cut at the chain's open
end leaves nothing to trace out — the reduced state is pure and
nothing is thermal. The heat *is* the traced-out half.)

Still blocked (stated): the accelerated-NODE protocol — a data
schedule realizing constant acceleration for a single filter, which
would make the temperature per-observer rather than per-region. No
principled schedule map yet.
