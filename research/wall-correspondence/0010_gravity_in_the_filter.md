# 0010 — What gravity is in the filter

> **AI-generated, not peer-reviewed.** The strategy inverted: define
> gravity in filter space first, measure it, and let the desired
> outcome choose the sibling's 4D geometry (their 0109 closes that
> loop). Code: `0010_gravity_in_the_filter.py`; the field theory is
> 0011.

> **⚖️ ATTRIBUTION —** _Defines "gravity" inside the filter by analogy (mass = absorption e^{−2I}, trust wells = Shapiro delay/lensing, equivalence principle = q·Var(posterior) response). The one genuine estimation fact is that linear response equals q·posterior-variance (a score/Fisher relation); the rest is metaphor mapping GR concepts onto filter internals._ Prior art: general relativity concepts (equivalence principle; Shapiro delay, Shapiro 1964; gravitational lensing); linear response = posterior variance is standard estimation. Status: SPECULATIVE (the gravity correspondence is an unestablished analogy).

**The definition.** Gravity is what accumulated information does to
the propagation of influence. Trust (precision) is the mass; the
precision channel is the metric. Three local laws, each measured:

1. **Inertia is precision; mass is absorption.** A node with
   information advantage I over its incoming channel transmits the
   fraction f = e^{−2I} of an incident influence and absorbs
   1 − e^{−2I} — the sibling's saturating mass law
   (m = (1 − e^{−I})/4G), derived from Bayes alone and verified to
   1e−12. A leaky node (process noise = finite memory) has bounded
   mass (w = 0.005 → mass_max 0.93); only a *rigid* (conserved)
   node absorbs without bound — f → 0, an asymptotic horizon that
   transmits nothing. Black holes are perfectly-remembering nodes.
2. **Trust wells delay and bend influence.** On a relay grid, a
   high-trust disk produces a Shapiro delay (+483 steps behind the
   well vs the clear row) while arrival behind it beats the
   through-well estimate by >2× — the influence routed *around*:
   geodesics avoid trust concentrations.
3. **Universality, exact form.** The response to a small influence
   is q·Var(posterior) *identically* — history and shape enter
   nowhere (Gaussian vs bimodal node: identity holds to 1e−8).
   Only the information state at the encounter gravitates; shape
   reappears, as it should, in the tides (large influences).

A first-run correction, kept per house policy: the naive statement
"same prior precision → same response" is *false* (13% off for the
bimodal node); the exact law is posterior-variance response. The
equivalence principle is a tangent-space theorem, exactly as in GR.

## Open
1. The field theory (0011): Poisson, dimension selection, waves.
2. The horizon thermodynamics: what does the rigid node's
   absorption spectrum look like against the sibling's Unruh result?
