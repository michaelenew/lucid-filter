# 0002 — The homomorphism: this family and the sibling are one object, minus four parts

> **AI-generated, not peer-reviewed** — correspondence record; the
> sibling's side is `quantum-mechanics` foundations/0098, with a
> verification module. This repository's cited results are its own.

## The theorems (verified on the sibling's side)

1. **The free tiers are isomorphic.** A Kalman predict step, written
   on Fourier modes, is e^{ikμ}·e^{−qk²/2}: drift in the phase, noise
   in the modulus. The sibling's ledger chain at heat-kernel weight is
   ω^{nf}·e^{−τn²}: the same polar transfer on a different group. One
   semigroup, two groups; the map is "swap the state-space group."
2. **Their renormalization step is a filter cycle.** Their bond move
   (pointwise power of the weight) is Bayes conditioning on parallel
   replicas; their decimation is predict. Their coupling τ is a
   posterior variance; their conjugate family (heat kernels) is this
   family's Gaussians. On ℝ — this family's state space — their 4D
   cycle is *exactly marginal*: β = 0.
3. **Their running coupling is the gap, located.** The same cycle on
   ℝ / U(1) / SU(2): β = 0 exactly / < 1e−6 / 0.127·τ². Their
   asymptotic freedom, emergent scale, and confinement are the residue
   of **noncommutative curvature of the state space** — the one
   ingredient this family's ℝ lacks. Compactness alone does nothing.

## What this repository's roadmap means in their terms

| this repository's item | what it builds, physically |
|---|---|
| marginalise the (φ_P, s_P) grid ("a hypothesis set is not a point") | their vacuum's missing scale-mixture — verified on their side: the collapsed vacuum erases sector structure (+0.004), the scale-mixture vacuum restores it (+0.50) |
| **regime-hazard in place of AR(1)** (the 6.8% stratum) | **their superselection sectors** — discrete hidden regimes are the data-side twin of their center/representation labels; pressing here builds the missing sector tier |
| a filter on a curved, noncommutative state space (directional statistics on S³ — not currently planned) | a filter with a *running regret*: the one structure that would let this family see their β = c·τ² from the data side |
| the innovation stream (what this family conditions on every step) | the thing their theory does not have: they only self-condition on internal replicas. "Who supplies the innovation" is their measurement/causality question — and this family's own 3.7% detection-lag stratum is the measured cost of exactly that layer |

## Status

Experiment 2's target stands (their c ≈ 0.13/blocking vs this
repository's regret-growth coefficient) — now with a sharper
prediction attached: on a flat state space the regret coefficient
should be **zero** (their Theorem 2); any nonzero running this family
measures in its own flat-space cascade is scheme artifact, not
physics. The curved-state-space prototype (S³) is the experiment that
would carry a genuine nonzero target.
