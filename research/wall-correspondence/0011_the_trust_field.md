# 0011 — The trust field: the 4D corollary

> **AI-generated, not peer-reviewed.** The 4D object built to the
> desired gravity outcome: a bank of streams on a 3-lattice sharing
> a pinned (massless) log-scale field; time is the stream index.
> Code: `0011_the_trust_field.py`.

Three measured statements:

1. **Newton's 1/r is the field's Green function — and only in three
   spatial dimensions.** A point information source induces a trust
   perturbation falling as 1/r^α with **α = 1.02** on the 3-lattice
   (free-exponent fit), logarithmically in 2D (residual 0.004),
   linearly in 1D (0.001). The bank's dimension *selects* the
   potential; demanding Newton demands 3+1. This is the
   back-into-the-outcome step: the 4D filter object is the one
   whose statics is Newtonian.
2. **The record-tier field is Newtonian-diffusive.** An information
   pulse spreads with front radius ~ t^0.52: statics, screening,
   and delay are right, but there is *no radiation* in the causal
   record tier. Ported prediction: **gravitational waves are a
   source-tier (phase-ledger) phenomenon** — the same record/source
   split that 0005/0006 measured for the Born structure. The filter
   is not failing to radiate; it is telling us which ledger
   radiates.
3. **One mass formula, node to field.** By Sherman–Morrison the
   field at a source of data-precision ρ is exactly
   λ₀·ρG₀₀/(1 + ρG₀₀) = λ₀(1 − e^{−2I}) with
   I = ½ln(1 + ρG₀₀): the node-level saturating mass law (0010 s1)
   *is* the field-level one, with the lattice Green function G₀₀
   playing the role their formula gives to 4G. The identity is
   exact (1e−12), not asymptotic.

**What this bought the physics**: the Gauss requirement — the
boundary of a region must read exactly the enclosed trust, or the
field theory above acquires fake sources — is the selection
principle that settles their 4D surface-ordering question (their
0109): lawful orderings satisfy it exactly and differ only by
gauge; unlawful ones fake mass and are excluded.

## Honest limits
- The field posterior is treated at its Gaussian mean level (exact
  for the Laplacian prior); the interacting corrections are the
  sibling's TC insertions (their 0107).
- "Demanding Newton demands 3+1" selects the *spatial dimension of
  the sharing graph*, given the Laplacian coupling; it does not yet
  derive the coupling form itself.

## Open
1. Two-source attraction dynamics (the force law from information
   flow, beyond the static potential).
2. The source-tier wave sector: couple the phase ledger to the
   trust field and look for radiation.
