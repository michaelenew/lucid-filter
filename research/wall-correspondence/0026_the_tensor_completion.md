# 0026 — The tensor completion: precision is a matrix, and comparing matrices needs a connection

> **AI-generated, not peer-reviewed.** The scalar completion's
> named residues (the factor 2 in r_h, the missing rotation sector)
> are what a tensor theory would supply. First steps, and the
> structure it turns out to require. Code:
> `0026_the_tensor_completion.py`.

- **The tensor equivalence principle** (exact, 0 error): a
  multi-dimensional node's response to a small incident influence
  is the **matrix** q·Cov(posterior) — 0010's scalar law with all
  indices restored. The object playing the metric is a symmetric
  positive-definite *tensor* field.
- **The naive completion overshoots.** Running the derived learning
  dynamics component-wise on that tensor field, **all 6 components
  (d = 3) propagate as independent massless fields** — identical
  −k² relaxation for every one — where gravity has 2. Nothing
  removes modes, because the naive construction has no gauge
  freedom.
- **What's missing is the connection — and it's already built.**
  Precision matrices at different nodes live in different frames;
  comparing them *requires* a transport R_xy. Build records from
  P_x versus R_xy P_y R_xyᵀ and local frame changes become an
  **exact gauge symmetry** (verified to 8e−15). The gauge-invariant
  content of the tensor field then collapses to its **eigenvalues**
  (6 components − 3 frame parameters = 3), and the transport's own
  closed-loop **holonomy** becomes an independent object.

**That object is the sibling's link variable, and its curvature is
their plaquette.** The tensor completion of filter gravity *is* a
gauge theory — and it is their gauge theory. The two programs,
which met at the isomorphism tiers and then diverged into a scalar
gravity, converge again at the tensor tier.

**The remaining gap, named precisely**: the transport field's own
dynamics — what action the connection obeys, which the scalar
completion never needed and which the physics side derives from its
Born weight. That is the next campaign, and it is now a single
well-posed question rather than an open direction.
