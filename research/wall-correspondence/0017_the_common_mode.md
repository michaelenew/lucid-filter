# 0017 — The common mode (F6): what survives differencing

> **AI-generated, not peer-reviewed.** Code: `0017_the_common_mode.py`.

> **⚖️ ATTRIBUTION —** _Standard variance-components result: the shared-level increments are identifiable with variance shrinking as 1/M, while the level's absolute value is an unidentifiable constant (a "zero mode"). The R² formula is elementary. Naming the unidentifiable constant a "cosmological-constant / Λ statement" is analogy._ Prior art: random-effects / variance-components models; identifiability of a common level. Status: RECOMBINATION (measurement matches exact prediction); the Λ correspondence is SPECULATIVE.

M streams share a slow level; everything is differenced. Measured
against the exact prediction R² = s_c²/(s_c² + (s_p² + 2s_y²)/M):
match to ≤0.015 at every M (4→256). **The level's increments are
identifiable at exactly the 1/M law; the level itself only up to an
arbitrary constant — the zero mode is not in the record.** The
bank-side Λ statement: the common level's absolute value is a
convention (the sibling's budget quantizes it globally, 0080/0094);
only its drift gravitates in the record. F6 closed as posed; the
residual-drift detectability threshold is the follow-up if their
Λ1 run ever needs a number.
