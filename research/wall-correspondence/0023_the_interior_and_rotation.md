# 0023 — The interior, the rotation sector, and the evaporation shape

> **AI-generated, not peer-reviewed.** The three opens left by the
> nonlinear completion. Code: `0023_the_interior_and_rotation.py`.

> **⚖️ ATTRIBUTION —** _Three analogy results: greybody/remnant floors from a finite level cutoff, "frame-dragging = order channel" (reusing 0009's non-commutativity), and dM/dt∝−1/M² ⟹ lifetime∝M₀³ — which is explicitly stated to be dimensional analysis (Stefan-type law with T=2/M, A∝M²), not a derived emission rate. Reproduces the shapes of Hawking evaporation by construction._ Prior art: Hawking evaporation scaling; greybody factors; black-hole remnants. Status: SPECULATIVE (analogy, self-labelled as dimensional analysis).

**The interior.** Beyond the capacitance nothing goes complex — those
masses are simply **unreachable**: M(s) rises to C and stops, no
source strength produces more. At the extremal limit ψ = 0 through
the whole body: infinite trust potential, zero transmission — the
singular interior, approached only in the limit. But at *finite
resolution* the horizon never quite closes. If the deepest
representable potential is λ_max, transmission floors at e^{−λ_max}
and the mass cap softens to (1 − e^{−λ_max})·C. Taking the sibling's
budget capacity λ_max = ln N (their 0100): a level-N web has minimum
transmission **1/N** and maximum mass **(1 − 1/N)·C** —
**discreteness replaces horizons with greybody floors** (conditional
on that identification, and labelled as such). For their N = 5:
transmission never falls below 20%.

**Rotation is the order channel.** The completion is scalar: its
field depends on the source *multiset* alone, so reversing
circulation leaves every scalar observable bitwise identical —
information about circulation in any scalar observable is **exactly
zero**. No frame-dragging, and not because a term was dropped.
What circulation *is*, in this program's own terms, is the **order
of records around a loop** — precisely the channel scalar/abelian
structure cannot carry (0009). Measured on a 4-element loop: the
ordered composite separates the two circulations by
⟨|Δθ|⟩ = 0.183 and carries **0.047 nats/loop**. Frame-dragging, if
this program has it, is order information sourcing a vector sector:
a named structure to build, not a missing term.

**The evaporation shape.** With T = 2/M and A ∝ M², a Stefan-type
law gives dM/dt = −(4c/π)/M² exactly, so **M³ is linear in time and
the lifetime ∝ M₀³** — Hawking's evaporation shape (verified by
integration: 261.8 vs 261.8, 2094.4 vs 2094.4). This is
**dimensional analysis, not a derived emission rate**; the carrier
is the source tier (0014), whose rate is not computed here. With the
greybody floor above, the last stage stalls: a level-N web leaves a
remnant.
