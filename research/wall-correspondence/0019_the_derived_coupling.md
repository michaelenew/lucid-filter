# 0019 — The coupling, derived: ratio records + pinned learning ⇒ Coulomb response

> **AI-generated, not peer-reviewed.** The campaign's capstone: the
> last posited piece of filter-gravity, derived — with three
> structural discoveries forced along the way. Code:
> `0019_the_derived_coupling.py`.

> **⚖️ ATTRIBUTION —** _Two genuine facts, dressed as gravity: (a) the response of a local averaging/learning operator (the graph Laplacian) to a point source is its Green function — 1/r in 3D — standard potential theory; (b) dimension selection is the Pólya recurrence/transience of random walks (3D transient, 1D/2D recurrent). "No FDT" restates that response ≠ fluctuation spectrum absent a fluctuation-dissipation relation (Callen–Welton 1951). All standard; the gravity narrative is analogy._ Prior art: Laplacian Green functions; Pólya 1921 (recurrence); Callen–Welton 1951. Status: SPECULATIVE (correspondence); underlying math REPRODUCTION.

**The chain**: freeze lemma (spatial records carry log-ratios) +
masslessness (no restoring force to an absolute level) + locality ⇒
the online bank's learning operator **is** the graph Laplacian ⇒ the
field's response to a persistent source is the Laplacian's Green
function ⇒ with 0011–0012, all of Newton. Measured: 3D steady
response 1/r^α with **α = 1.00**; 1D exact tent (residual 0.000).
No prior is posited anywhere — *the potential is the learning
operator's Green function.*

**The three discoveries:**
1. **Mass sources through time.** A purely spatial ratio-bias is
   satisfiable (λ = b at the source solves every bond record — a
   delta, no tail). The point charge must enter through the
   *temporal* ratio channel: a site whose innovations say "my scale
   rose relative to my own past" — exactly 0010's mass =
   own-history information, and Sherman–Morrison then yields the
   saturating mass law automatically.
2. **A well needs an open boundary.** In a closed (periodic) box the
   pin lifts the *whole* field to its level — no well (measured:
   range 0.0015 around mean 0.999). The potential is sustained by
   flux to infinity; and only 3D sustains it at infinite volume
   (diffusion transient in 3D, recurrent in 1D/2D) — the dimension
   selection, in dynamical form. Echoes the sibling's closed
   universe = innovation-free filter.
3. **No FDT.** The same dynamics' vacuum spectrum is *white*
   (cv 0.17): response is Coulomb, fluctuations are not — gravity is
   a response phenomenon. This reconciles Newton with the measured
   short-ranged vacuum scale correlations (their 0102/0104) and is a
   falsifiable prediction: the trust field violates
   fluctuation-dissipation.

**The control**: sum records (explaining-away) also have a massless
mode — at the **zone corner**: staggered, long-ranged anti-binding
(sign product −1.0, r = 20 amplitude 0.69 of source). The record
structure selects the massless momentum; smooth universal attraction
selects the ratio channel — the freeze lemma's own structure.

Open: exhibit the lattice trust record as ratio-valued at the field
level (the freeze lemma is proven on the ledger tier); ambient
memory density as a screening mass (Yukawa range 1/√ρ₀ — a
prediction in search of an observable).
